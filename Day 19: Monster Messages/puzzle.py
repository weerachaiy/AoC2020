import sys

puzzle = [line for line in open(sys.argv[1]).read().split("\n\n")]
rules = [s.split(":") for s in puzzle[0].split("\n")]
rule = dict(rules)
n = 0
while True:
  x = str(n)
  rule[x].split())
  break
